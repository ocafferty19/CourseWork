
//used in poly.c and polyapp.c
//declaration for all functions in poly.c
int addPolyTerm(int c,int e);//return -1 or 0 depending if the PolyTerm was added. Obviously 0 for success.
void displayPolynomial();// sorts the polynomial in ascending order by exponentials and adds polynomial with same expo. Then prints the polynomial with changes
int evaluatePolynomial(int x); //evaluates polynomial at x value and returns y value
